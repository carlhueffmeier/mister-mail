{
  "version" : "2017-02-28",
  "operation" : "Query",
  "query" : {
    "expression" : "pk = :uid and begins_with(sk, :prefix)",
    "expressionValues" : {
      ":uid": $util.dynamodb.toDynamoDBJson($context.identity.sub),
      ":prefix": { "S": "C-" }
    }
  },
  "select" : "ALL_ATTRIBUTES"
}
